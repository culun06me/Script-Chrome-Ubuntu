# Chrome install for Linux

![Chrome](Chrome.png)

Bash script to install the latest version of `Chrome` on Linux with desktop integration from Chrome.

## Run

``` shell
curl -s -L linux-install-chrome 

```

